# Kernel boundary

No production Kernel code exists yet by design. The Kernel will be derived from three real standalone Capsule APIs, not invented as a framework that Capsules must implement.

See `ARCHITECTURE.md` and `docs/08-kernel-and-adapters.md`.

