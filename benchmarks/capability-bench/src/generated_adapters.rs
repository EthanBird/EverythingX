// Generated by tools/build_performance_harness.py; do not edit.
use everythingx_kernel::Kernel;
use everythingx_protocol::{AdapterHandshake, StaticAdapter};

pub const ADAPTER_COUNT: usize = 64;

pub fn register_all(kernel: &mut Kernel) -> Vec<AdapterHandshake> {
    let mut handshakes = Vec::new();
    let adapter = adapter_000::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_001::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_002::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_003::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_004::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_005::AiffPcmToWavPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_006::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_007::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_008::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_009::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_010::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_011::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_012::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_013::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_014::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_015::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_016::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_017::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_018::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_019::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_020::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_021::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_022::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_023::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_024::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_025::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_026::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_027::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_028::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_029::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_030::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_031::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_032::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_033::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_034::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_035::RawPcmToWavPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_036::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_037::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_038::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_039::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_040::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_041::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_042::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_043::WavPcmToAiffAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_044::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_045::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_046::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_047::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_048::WavPcmToRawPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_049::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_050::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_051::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_052::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_053::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_054::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_055::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_056::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_057::GeneratedPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_058::GeneratedRawPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_059::GeneratedRawPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_060::GeneratedRawPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_061::GeneratedRawPcmAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_062::BmpToPngAdapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    let adapter = adapter_063::Utf16ToUtf8Adapter;
    handshakes.push(adapter.handshake());
    kernel.register(Box::new(adapter)).expect("generated Adapter registry must be unique");
    handshakes
}
