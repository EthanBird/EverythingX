[package]
name = "gif-render-frame-to-png"
version = "0.1.0"
edition = "2024"
publish = false
license-file = "LICENSE"
description = "Render one explicitly indexed composited GIF animation frame to PNG."

[lib]
path = "src/lib.rs"

[dependencies]
