[package]
name = "qoi-to-tga"
version = "0.1.0"
edition = "2024"
publish = false
license-file = "LICENSE"
description = "Standalone zero-dependency Quite OK Image to Truevision TGA raster conversion"

[lib]
path = "src/lib.rs"

[dependencies]
