[package]
name = "bmp-to-gif"
version = "0.1.0"
edition = "2024"
publish = false
license-file = "LICENSE"
description = "Windows BMP raster to GIF87a/GIF89a exact-palette still raster"

[lib]
path = "src/lib.rs"

[dependencies]
