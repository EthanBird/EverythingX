[package]
name = "qoi-to-bmp"
version = "0.1.0"
edition = "2024"
publish = false
license-file = "LICENSE"
description = "Standalone zero-dependency Quite OK Image to Windows BMP raster conversion"

[lib]
path = "src/lib.rs"

[dependencies]
