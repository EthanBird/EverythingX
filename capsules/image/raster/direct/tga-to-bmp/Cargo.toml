[package]
name = "tga-to-bmp"
version = "0.1.0"
edition = "2024"
publish = false
license-file = "LICENSE"
description = "Standalone zero-dependency Truevision TGA raster to Windows BMP raster conversion"

[lib]
path = "src/lib.rs"

[dependencies]
