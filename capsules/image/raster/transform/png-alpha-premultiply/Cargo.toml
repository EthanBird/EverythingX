[package]
name = "png-alpha-premultiply"
version = "0.1.0"
edition = "2024"
publish = false
license-file = "LICENSE"
description = "Convert unassociated PNG RGB samples to premultiplied code values with defined integer rounding."

[lib]
path = "src/lib.rs"

[dependencies]
