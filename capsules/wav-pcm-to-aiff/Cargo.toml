[package]
name = "wav-pcm-to-aiff"
version = "0.1.0"
edition = "2024"
publish = false
license-file = "LICENSE"
description = "Standalone, zero-dependency WAV PCM to AIFF conversion library"

[lib]
path = "src/lib.rs"

[dependencies]

