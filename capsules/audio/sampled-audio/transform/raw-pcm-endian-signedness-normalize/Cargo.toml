[package]
name = "raw-pcm-endian-signedness-normalize"
version = "0.1.0"
edition = "2024"
publish = false
license-file = "LICENSE"
description = "Standalone zero-dependency Byte-order and integer signedness normalization of raw PCM samples."

[lib]
path = "src/lib.rs"

[dependencies]
