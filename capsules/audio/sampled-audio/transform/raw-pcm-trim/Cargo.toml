[package]
name = "raw-pcm-trim"
version = "0.1.0"
edition = "2024"
publish = false
license-file = "LICENSE"
description = "Standalone zero-dependency Frame-exact trimming of parameterized raw interleaved PCM."

[lib]
path = "src/lib.rs"

[dependencies]
