[package]
name = "raw-pcm-to-wav-pcm"
version = "0.1.0"
edition = "2024"
publish = false
license-file = "LICENSE"
description = "Standalone, zero-dependency raw integer PCM to WAV PCM conversion library"

[lib]
path = "src/lib.rs"

[dependencies]
