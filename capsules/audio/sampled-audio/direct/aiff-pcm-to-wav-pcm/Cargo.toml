[package]
name = "aiff-pcm-to-wav-pcm"
version = "0.1.0"
edition = "2024"
publish = false
license-file = "LICENSE"
description = "Standalone, zero-dependency AIFF PCM to WAV PCM conversion library"

[lib]
path = "src/lib.rs"

[dependencies]
