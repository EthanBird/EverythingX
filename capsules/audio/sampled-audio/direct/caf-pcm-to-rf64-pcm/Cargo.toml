[package]
name = "caf-pcm-to-rf64-pcm"
version = "0.1.0"
edition = "2024"
publish = false
license-file = "LICENSE"
description = "Standalone zero-dependency Core Audio Format PCM to RF64 PCM PCM converter"

[lib]
path = "src/lib.rs"

[dependencies]
